{
  "name": "kojiki-subgraph",
  "version": "1.0.0",
  "license": "GPL-3.0-or-later",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy": "graph deploy --studio kojiki-test --deploy-key 03acd7e9a0c7da2631bb6a713075ea00",
    "create-local": "graph create --node http://localhost:8020/ kojiki-subgraph",
    "remove-local": "graph remove --node http://localhost:8020/ kojiki-subgraph",
    "deploy-local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 kojiki-subgraph"
  },
  "devDependencies": {
    "@graphprotocol/graph-cli": "0.46.1",
    "@graphprotocol/graph-ts": "0.29.1",
    "@typescript-eslint/eslint-plugin": "^2.0.0",
    "@typescript-eslint/parser": "^2.0.0",
    "eslint": "^6.2.2",
    "eslint-config-prettier": "^6.1.0",
    "prettier": "^1.18.2"
  }
}
